设计思想与需求背景：
- d1vai 在最近 25 次更新里，核心变化集中在「模型选择链路」和「workspace 就绪门控」：创建项目前选模型、聊天时可切模型、workspace 未就绪时延迟加载模型、模型切换时禁止发送、并把用户缓存模型自动同步到后端。
- d1vai_app 目前已具备 workspace 预热、消息队列和重连基础能力，但缺少完整的模型配置闭环（读取/切换/缓存/同步/发送时携带），导致移动端与 Web 行为不一致。
- 本次目标：在不引入仅 Web 场景逻辑的前提下，把 d1vai_app 对齐到同一套「可感知状态 + 可恢复流程 + 可追踪反馈」体验；每完成一个小功能都先跑检查，再在下面勾选。

Todo（逐项实现，逐项勾选）：
- [x] 新增模型配置 API 能力，并在 workspace ready 后自动同步本地缓存模型（最佳努力，不阻塞主流程）
- [x] 创建项目对话框增加模型选择器（workspace 未 ready 时 loading、失败自动重试、缓存回填）
- [x] 创建项目时应用当前选中模型（失败不阻断创建，但有明确提示）
- [x] 项目 Chat 顶栏增加模型选择器（含切换 loading、失败提示、缓存持久化）
- [x] Chat 发送链路接入模型门控：模型加载/切换中禁止发送；队列新增 waitingModel 状态
- [x] Chat executeSession/retry 请求携带 selected model，对齐 Web
- [x] 取消任务时展示明确的 cancelled 系统消息（非 web-only）

共识（保留）：
- 涉及前端 UI/交互：必须跑一次检查再打勾
- 任何代码改动：必须跑一次检查并修到 0 error（仓库强制要求）
- 每个设计前端的 task 需要专门设计用户体验，让关键流程有仪式感，完善 toast 让步骤 “有反馈”， 完善 loading 态， 让用户清晰流程
